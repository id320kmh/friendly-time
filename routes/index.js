const public = require('./public');
const home = require('./home');
const notFound = require('./notFound');
const pratsya = require('./pratsya');

module.exports = {
    public,
    home,
    notFound,
    pratsya
}